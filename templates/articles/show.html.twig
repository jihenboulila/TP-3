{% extends 'base.html.twig' %}

{% block title %}{{article.designation}}{% endblock %}

{% block body %}
<a href="/articles" class="btn btn-primary">Retour au articles</a>
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{article.designation}}</h5>
    <p class="card-text">{{article.Description}}</p>
    <p class="card-text text-danger">{{article.prix}}</p>
    <a href="#" class="btn btn-primary">Acheter</a>
  </div>
</div>

{% endblock %}
